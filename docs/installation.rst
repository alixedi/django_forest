============
Installation
============

At the command line::

    $ easy_install django_forest

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_forest
    $ pip install django_forest